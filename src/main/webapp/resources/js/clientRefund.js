/**
 * clientRefund
 */

$(document).ready(function() {
	
	// selectbox 년, 월 적용
	var today = new Date();
	var year = today.getFullyear();
	var month = today.getMonth() + 1;
	var registerYear; // 가입일 년도
	
	var str;
	
	// 가입일 기준으로 현재 년도까지
	for(var i = year; i > = registerYear; i--) {
		
	}
	
});