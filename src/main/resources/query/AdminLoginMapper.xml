<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fly.admin.login.dao.AdminLoginDao">

	<!-- 회원 가입 수 -->
	<select id="registCnt" parameterType="String" resultType="int">
		select count(*) 
		from member 
		<if test='date == "일"'>
			where to_char(m_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')
		</if>
		<if test='date == "월"'>
			where to_char(m_regdate, 'yy/MM') = to_char(sysdate, 'yy/MM')
		</if>
	</select>
	
	<!-- 구장 등록 수 -->
	<select id="placeCnt" parameterType="String" resultType="int">
		select count(*)
		from place
		<if test='date == "일"'>
			where to_char(p_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')
		</if>
		<if test='date == "월"'>
			where to_char(p_regdate, 'yy/MM') = to_char(sysdate, 'yy/MM')
		</if>
	</select>
	
	<!-- 경기장 예약 수 -->
	<select id="rentalCnt" parameterType="String" resultType="int">
		select count(*)
		from rental
		<if test='date == "일"'>
			where to_char(r_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')
		</if>
		<if test='date == "월"'>
			where to_char(r_regdate, 'yy/MM') = to_char(sysdate, 'yy/MM')
		</if>
	</select>
	
	<!-- 매치 게시판 등록 수 -->
	<select id="matchCnt" parameterType="String" resultType="int">
		select count(*)
		from match_board
		<if test='date == "일"'>
			where to_char(mb_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')
		</if>
		<if test='date == "월"'>
			where to_char(mb_regdate, 'yy/MM') = to_char(sysdate, 'yy/MM')
		</if>
	</select>
	
	<!-- 용병 게시판 등록 수 -->
	<select id="supportCnt" parameterType="String" resultType="int">
<<<<<<< HEAD
		select 
			(select count(*) 
			from support 
			where to_char(hs_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')) + 
			(select count(*) 
			from recruit 
			where to_char(hr_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd'))
		from dual
=======
		select count(*)
		from match_board
		<if test='date == "일"'>
			where to_char(mb_regdate, 'yy/MM/dd') = to_char(sysdate, 'yy/MM/dd')
		</if>
		<if test='date == "월"'>
			where to_char(mb_regdate, 'yy/MM') = to_char(sysdate, 'yy/MM')
		</if>
>>>>>>> ba2e6cb36b8205136210280cd234de563b39e162
	</select>

</mapper>